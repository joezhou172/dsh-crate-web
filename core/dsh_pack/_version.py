"""DSH Crate version used by the diagnostic protocol.

This value MUST match package.json version. The release workflow enforces the
sync so a diagnostic always names the Crate generation that produced it.
"""

__version__ = "0.1.1"
